import React from 'react';
import css from "./Posts.module.css"

const Posts = () => {
    return (
        <div className={css.posts}>
            posty
        </div>
    );
};

export default Posts;